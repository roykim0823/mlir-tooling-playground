# Autogenerated from /workspaces/polyglot_devcontainer/project/mlir-tutorial/tutorials/lit-and-filecheck/example/test/lit.site.cfg.py.in
# Do not edit!

# Allow generated file to be relocatable.
import os
import platform
def path(p):
    if not p: return ''
    # Follows lit.util.abs_path_preserve_drive, which cannot be imported here.
    if platform.system() == 'Windows':
        return os.path.abspath(os.path.join(os.path.dirname(__file__), p))
    else:
        return os.path.realpath(os.path.join(os.path.dirname(__file__), p))


# Absolute paths discovered by CMake are baked in here, in the BUILD dir.
config.llvm_tools_dir = lit_config.substitute("/workspaces/polyglot_devcontainer/project/mlir-tutorial/externals/llvm-project/build/./bin")
config.example_obj_root = "/workspaces/polyglot_devcontainer/project/mlir-tutorial/tutorials/lit-and-filecheck/example/build"

import lit.llvm
lit.llvm.initialize(lit_config, config)

# Hand off to the real, hand-written config that lives in the source tree.
lit_config.load_config(config, "/workspaces/polyglot_devcontainer/project/mlir-tutorial/tutorials/lit-and-filecheck/example/test/lit.cfg.py")
